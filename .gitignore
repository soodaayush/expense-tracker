node_modules/
dist/
.azurite/
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# secrets — never commit
# Unqualified (not "api/...") so it covers every Functions project (api/, notifications-worker/, …).
local.settings.json
.env
.env.local

# OS/editor
.DS_Store
.vscode/
